$("#photo-insert").append("<%= j render partial: "garage_photo", locals: {photo: @photo}%>");
tags = JSON.parse($(".current_tokens").attr("data-tokens"));
$(".garage_photo_tags").select2({tags:tags, tokenSeparators: [","]});
