<% if @photo.new_record? %>
  alert("Failed to upload painting: <%= j @photo.errors.full_messages.join(', ').html_safe %>");
<% else %>
  alert("rendering this shit!");
  $("#photos").append("<h1>Hello World!</h1>")
  $("#photos").append("<%= j render(@photo) %>");
<% end %>